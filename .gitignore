# Secrets — never commit
.env
*.key

# Local infra config (contains ADO org / infra names — keep out of the public repo)
.vscode/mcp.json

# Copyright — verbatim source text (personal grounding only; never publish).
# Only the transformative reference/ abridgements ship.
artifacts/**/extraction/
artifacts/**/source/

# Generated media
output/
# Storage Plane: a directory junction into "OneDrive - Sequitur Solutions" (the
# output store). Never commit — git must not traverse into OneDrive.
output-store/
*.mp4
*.mov
*.png
*.jpg
*.jpeg
!docs/**/*.png
!docs/**/*.jpg

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
*.code-workspace